<h1>Books</h1>
<% @books.each do |book| %>
<table>
　<tr>
   <th>title</th>
   <th>body</th>
  </tr>
   <tr></tr>
   <td><%= book.title %></td>
   <td><%= book.body %></td>
   <td>show</td>
   <td>edit</td>
   <td>Destroy</td>
</table>
<% end %>

<h1>New Book</h1>
<%= form_with model: @book, url:'/books', local:true do |f| %>
 <h4>title</h4>
 <%= f.text_field :title %>

 <h4>body</h4>
 <%= f.text_area :body %>
 <div class=button><%= f.submit 'Create Book' %></div>

<% end %>